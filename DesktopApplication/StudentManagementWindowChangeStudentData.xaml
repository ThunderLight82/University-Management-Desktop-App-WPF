<Page x:Class="DesktopApplication.StudentManagementWindowChangeStudentData"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StudentManagementWindowChangeStudentData">

    <Grid>

        <StackPanel Margin="5">
            <TextBlock Text="Change existed student info" FontWeight="Bold" HorizontalAlignment="Left" Margin="5"  />
            <TextBlock Text="Select student from list of students below, then modify student data in fields" HorizontalAlignment="Left" Margin="5,0" />
            <TextBlock Text="and then click 'Change Student Info' button below student list:" HorizontalAlignment="Left" Margin="5,0" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBox Name="ChangeStudentFullNameTextBox" Width="220" HorizontalAlignment="Left" Margin="5,5" Height="25">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="FontSize" Value="15" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Grid>
                                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}" />
                                            <ScrollViewer x:Name="PART_ContentHost" />
                                            <TextBlock Name="PlaceholderForFutureName" Text="Select student to change name here" FontSize="13"
                                                   Foreground="Gray" Margin="2,0,0,0" />
                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                <Setter TargetName="PlaceholderForFutureName" Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="{x:Null}">
                                                <Setter TargetName="PlaceholderForFutureName" Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                            <Trigger Property="IsFocused" Value="True">
                                                <Setter TargetName="PlaceholderForFutureName" Property="Visibility" Value="Collapsed" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <ComboBox Name="IsWorkingComboBox"  Text="Working?" Width="220" IsEditable="True" HorizontalAlignment="Left" Margin="5" SelectedItem="{Binding Path=isWorkingInDepartment, Mode=TwoWay}" >
                    <ComboBoxItem Content="Yes" IsSelected="True" />
                    <ComboBoxItem Content="No" />
                </ComboBox>

            </StackPanel>

        </StackPanel>

        <ListView Name="StudentsListView" ItemsSource="{Binding Students}" Width="450" Height="350" HorizontalAlignment="Center" Margin="0,35,0,0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding StudentId}" />
                    <GridViewColumn Header="Full Name" Width="150" DisplayMemberBinding="{Binding StudentFullName}" />
                    <GridViewColumn Header="Current Group" Width="120" DisplayMemberBinding="{Binding CurrentGroupName}" />
                    <GridViewColumn Header="Working" Width="50" DisplayMemberBinding="{Binding IsWorkingInDepartment}" />
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="20">
            <Button Content="Change Student Info" Click="ChangeStudentNameAndWorkInfo_Click" Width="200" Height="35" Margin="15,0,15,-5" HorizontalAlignment="Center" VerticalAlignment="Bottom" />
        </StackPanel>

    </Grid>
</Page>
